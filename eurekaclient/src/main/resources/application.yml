server:
    port: 8010
#    port: 8011
spring:
    application:
        name: provider  # 当前服务在注册中心上的名称
eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/  # 注册中心默认访问地址
    instance:
        prefer-ip-address: true # 是否将当前服务的ip注册到注册中心

