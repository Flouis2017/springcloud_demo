server:
    port: 8761  # 指定注册中心端口号，注意注册中心本身也是一个微服务
eureka:
    client:
        register-with-eureka: false     # 是否将Eureka Server作为客户端进行注册
        fetch-registry: false           # 是否同步其他注册中心的数据
        service-url:
            defaultZone: http://localhost:8761/eureka/

